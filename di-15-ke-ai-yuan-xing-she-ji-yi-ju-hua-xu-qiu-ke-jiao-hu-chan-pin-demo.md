# 第 15 课｜AI 原型设计：一句话需求 → 可交互产品 Demo



## 第二阶段：产品设计 × Python 实战

### 第 15 课｜AI 原型设计：一句话需求 → 可交互产品 Demo

> 🕐 **课程时长：45 分钟**\
> 🎯 **前置知识：已完成第 01～14 课，了解 Streamlit 基础**\
> 🛠️ **工具准备：Python 3.x、Streamlit、ChatGPT / Claude 等 AI 助手**

***

### 课程目标

完成本课后，你可以：

1. 把一句产品需求拆成简单的页面结构。
2. 使用 AI 生成 Streamlit 原型代码。
3. 在本地运行一个可点击的产品 Demo。
4. 通过自然语言继续修改原型。

本课不追求生产级前端。

目标只有一个：

```
产品需求 → AI 生成代码 → Streamlit 运行 → 点击验证 → 修改需求
```

***

### 🔥 1. 为什么需要 AI 原型？（5 分钟）

产品需求只有文字时，很多问题很难在评审阶段发现。

例如：

> 做一个用户管理后台，可以搜索用户，并显示用户总数、今日新增和风险用户数量。

传统方式可能需要先画原型，再调整页面。

现在可以直接：

```
一句需求
   ↓
AI 生成 Streamlit
   ↓
本地运行
   ↓
得到可点击 Demo
```

这里生成的不是正式产品，而是一个 **概念验证 Demo（Prototype）**。

它适合：

* 需求评审
* 页面结构验证
* 交互流程演示
* 和研发快速沟通

***

### 🎯 2. 把需求描述清楚（5 分钟）

AI 能不能生成好原型，很大程度取决于需求是否清楚。

不要只写：

```
帮我做一个后台。
```

可以写成：

```
请使用 Streamlit 创建一个用户管理后台。

要求：
1. 页面标题：用户管理
2. 顶部显示三个指标：
   - 用户总数
   - 今日新增
   - 风险用户
3. 增加一个用户搜索框
4. 点击查询按钮后显示搜索结果
5. 页面使用中文
6. 代码保持简单，适合 Python 初学者
```

#### 一个简单公式

描述原型时，可以按照：

```
页面是什么
+
页面有什么
+
用户可以做什么
```

例如：

```
用户管理后台
+
指标卡片、搜索框
+
搜索用户
```

***

### 💻 3. 第一个 Streamlit 原型（10 分钟）

让 AI 根据上面的需求生成代码。

保存为：

```
app.py
```

代码：

```python
import streamlit as st

st.set_page_config(
    page_title="用户管理",
    page_icon="👤",
    layout="wide"
)

st.title("👤 用户管理")

# 核心指标
col1, col2, col3 = st.columns(3)

col1.metric("用户总数", "1,280")
col2.metric("今日新增", "32")
col3.metric("风险用户", "18")

st.divider()

# 用户搜索
st.subheader("🔍 用户查询")

keyword = st.text_input(
    "输入用户 ID 或邮箱",
    placeholder="例如：10001"
)

if st.button("查询"):
    if keyword:
        st.success(f"找到用户：{keyword}")
    else:
        st.warning("请输入用户 ID 或邮箱")
```

***

### ▶️ 4. 运行 Demo（5 分钟）

安装 Streamlit：

```bash
pip install streamlit
```

启动：

```bash
streamlit run app.py
```

浏览器通常会自动打开：

```
http://localhost:8501
```

现在已经得到一个可以点击的产品原型。

***

### 🧩 5. 理解代码（8 分钟）

这一课只需要理解几个 Streamlit 组件。

#### 标题

```python
st.title("用户管理")
```

显示页面标题。

#### 分栏

```python
col1, col2, col3 = st.columns(3)
```

把页面分成三列。

#### 指标卡

```python
col1.metric("用户总数", "1,280")
```

适合展示 KPI。

#### 输入框

```python
keyword = st.text_input("输入用户 ID")
```

获取用户输入。

#### 按钮

```python
if st.button("查询"):
    st.success("查询成功")
```

用户点击按钮后执行操作。

这几个组件已经足够完成很多简单的后台原型。

***

### 🤖 6. 用自然语言继续修改（7 分钟）

原型最重要的不是第一次就做到完美，而是快速迭代。

例如第一版完成后，可以继续告诉 AI：

```
在当前代码基础上修改：

1. 搜索框增加邮箱查询提示
2. 查询结果增加风险等级
3. 增加账户状态
4. 不要重写整个项目
5. 保持代码简单
```

还可以继续：

```
增加一个“冻结账户”按钮。
```

或者：

```
顶部增加“今日登录用户”指标。
```

整个过程就是：

```
需求
 ↓
第一版 Demo
 ↓
发现问题
 ↓
自然语言修改
 ↓
第二版 Demo
```

这比一开始追求完整系统更适合原型阶段。

***

### 🛡️ 7. 原型和正式系统的区别（2 分钟）

本课 Demo 中的数据：

```python
col1.metric("用户总数", "1,280")
```

都是演示数据。

真正的生产系统通常还需要：

```
数据库
API
登录认证
权限控制
异常处理
日志
测试
部署
```

这些不是本课重点。

本课只解决：

> **能不能快速把产品想法变成一个可以点击、可以讨论的界面。**

***

### 🎓 课程小结（3 分钟）

本课完成了一条非常实用的 AI 产品工作流：

```
自然语言需求
      ↓
AI 生成 Streamlit
      ↓
运行 app.py
      ↓
浏览器查看 Demo
      ↓
点击验证
      ↓
继续告诉 AI 修改
```

#### 本课核心组件

```
st.title()
    页面标题

st.columns()
    页面分栏

st.metric()
    KPI 指标

st.text_input()
    用户输入

st.button()
    点击交互
```

不需要：

```
❌ Claude / OpenAI API 接入
❌ exec() 动态执行代码
❌ 正则提取 AI 代码
❌ session_state 状态管理
❌ 自动生成 Python 文件
❌ 复杂前端框架
```

先掌握：

> **需求 → AI → Streamlit → Demo**

就足够了。

***

### 🎯 课后作业

#### 练习 1：增加指标

在页面顶部增加：

```
今日登录用户：856
```

最终变成四个指标卡。

#### 练习 2：完善查询结果

用户点击查询后显示：

```
UID：10001
邮箱：demo@example.com
账户状态：正常
风险等级：低风险
```

#### 练习 3：让 AI 修改原型

把当前 `app.py` 交给 AI，并告诉它：

```
在当前页面下面增加一个用户列表。

字段：
UID
邮箱
注册时间
账户状态
风险等级

请保持现有功能不变，并保持代码简单。
```

运行修改后的代码，观察页面变化。

***

### ✅ 本课交付物

完成课程后，项目只需要：

```
prototype-demo/
└── app.py
```

运行：

```bash
streamlit run app.py
```

即可展示你的第一个 AI 产品原型。

***

_第二阶段：产品设计 × Python 实战 · 第 15 课_
